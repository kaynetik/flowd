{
  "_note": "Reference snapshot. Merge into ~/.claude/settings.json (deep-merge mcpServers and hooks so existing entries are preserved). The 'flowd' binary must be on $PATH -- `cargo install --path crates/flowd-cli` puts it in ~/.cargo/bin. No /ABSOLUTE/PATH substitution is required; no bash, jq, or uuidgen dependency.",
  "mcpServers": {
    "flowd": {
      "command": "flowd",
      "args": ["start"],
      "env": {
        "RUST_LOG": "flowd=info"
      }
    }
  },
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          { "type": "command", "command": "flowd hook session-start" }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "*",
        "hooks": [
          { "type": "command", "command": "flowd hook post-tool-use" }
        ]
      }
    ],
    "SessionEnd": [
      {
        "matcher": "*",
        "hooks": [
          { "type": "command", "command": "flowd hook session-end" }
        ]
      }
    ]
  }
}
