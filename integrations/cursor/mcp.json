{
  "_note": "Reference snapshot. The supported install path is `flowd init cursor --global` (or `--project <path>`), which deep-merges the canonical stanza below into your existing mcp.json and pins the `command` to the absolute path of the running flowd binary. Use this file as a manual-merge fallback only; when merged by hand, ensure the `flowd` binary is on $PATH.",
  "mcpServers": {
    "flowd": {
      "command": "flowd",
      "args": ["start"],
      "env": {
        "RUST_LOG": "flowd=info"
      }
    }
  }
}
